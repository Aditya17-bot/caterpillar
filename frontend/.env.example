VITE_API_URL=http://localhost:8000
# Optional: operator login registry in Convex (run `npx convex dev` in frontend/ to get this URL).
# Leave unset to log in with the operators from the FastAPI backend.
# VITE_CONVEX_URL=https://<your-deployment>.convex.cloud
